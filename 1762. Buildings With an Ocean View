class Solution:
  def findBuildings(self, heights: List[int]) -> List[int]:
    #Solution 1: O(n) / O(n)
    prev = 0
    sol = []
    for i in range(len(heights)-1, -1, -1):
      if heights[i] > prev:
        sol.append(i)
        prev = heights[i]
    sol.reverse()
    return sol
