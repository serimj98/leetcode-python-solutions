class Solution:
  def addToArrayForm(self, num: List[int], k: int) -> List[int]:
    #Solution 1: O(n) / O(n)
    numStr = ""
    for i in num:
      numStr += str(i)
    return list(str(int(numStr) + k))
