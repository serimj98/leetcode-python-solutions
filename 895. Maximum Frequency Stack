class FreqStack:

  def __init__(self):
    self.stack = []
    self.freq = defaultdict(int)

  def push(self, x: int) -> None:
    self.stack.append(x)
    self.freq[x] += 1

  def pop(self) -> int:
    #find most frequent element
    freqEle, freqCount = None, 0
    for i in self.freq:
      if self.freq[i] > freqCount:
        freqEle, freqCount = i, self.freq[i]
    for i in range(len(self.stack)-1, -1, -1):
      if self.stack[i] == freqEle:
        self.stack = self.stack[:i] + self.stack[i+1:]
    self.freq[freqEle] -= 1
    return freqEle

# Your FreqStack object will be instantiated and called as such:
# obj = FreqStack()
# obj.push(x)
# param_2 = obj.pop()
